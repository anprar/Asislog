# Rust build output — never commit (portable binary built in CI)
target/
# Cargo lock: keep for binary crate (reproducible CI), so NOT ignored.

# AsisLog runtime sidecars / caches (per-file, regenerable, user-local)
*.asisidx
*.asislog.json

# Editor / OS noise
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# Local test / benchmark artifacts
*.profraw
perf-*.log
bench-data/
# Portable release archives (rebuilt by CI release job, never commit binaries)
*.zip
# Staged release binaries + checksums (rebuilt locally via README steps)
asislog-windows.exe
asislog-linux
dist/
# Scratch measurement logs at repo root (bench*.txt, mem*.txt, ...)
bench*.txt
mem*.txt
gen*.txt
check*.txt
clippy*.txt
err_summary.txt
